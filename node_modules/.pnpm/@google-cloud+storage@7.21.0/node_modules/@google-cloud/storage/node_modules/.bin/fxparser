#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../fast-xml-parser/src/cli/cli.js" "$@"
else
  exec node  "$basedir/../../../../fast-xml-parser/src/cli/cli.js" "$@"
fi
# cmd-shim-target=/Users/mikebrisk/CODE/AccuWeather/design-kanban/node_modules/.pnpm/@google-cloud+storage@7.21.0/node_modules/fast-xml-parser/src/cli/cli.js
